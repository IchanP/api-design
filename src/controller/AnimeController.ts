import { inject, injectable } from 'inversify';
/*
@injectable()
export class AnimeController {
  #service: IAnimeService;
  constructor (
    //     @inject('AnimeService') service: IAnimeService
  ) {
  //  this.#service = service;
  }
} */
